package com.mirdar.dataDeal;

public class Divide {

    public ArrayList<Cluster> divideCluster(ArrayList<Cluster> clusters)
    {
        ArrayList<Cluster> clusterList = new ArrayList<Cluster>();
        for(int i=0;i<clusters.size().i++)
        {
            if(clusters.get(i).needSplit())
            {
                if(clusters.get(i).splitType == 1)
                {

                }
                else
                {

                }
            }
        }
    }
    //在满足划分成500*500的子类条件下，划分产生的子类之间的PDRs数量差异最小
    public void splitType1(Cluster cluster)
    {

    }

    public void splitType2(Cluster cluster)
    {

    }
}
